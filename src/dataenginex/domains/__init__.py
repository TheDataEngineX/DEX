"""Specialized operations over foundation types (§5.3).

Data, analytics, ML, AI, governance, and security capabilities expressed as
typed operations. Deliberately re-exports nothing: importing one domain should
not drag in the dependencies of every other, and a package that eagerly imports
its children makes the §5.5 dependency rule impossible to test.
"""
