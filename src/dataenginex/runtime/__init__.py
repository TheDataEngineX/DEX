"""Runtime layer — control plane and execution (§5.3).

Depends on ``foundation`` contracts, never on ``interfaces`` or ``application``.
Concrete providers are injected at ``bootstrap``, not imported here.
"""
